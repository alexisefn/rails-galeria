<h1>Todos los Pins</h1>

<p><%= link_to "Nuevo Pin", new_pin_path, class: "button" %></p>

<div class="pins-container">
  <% @pins.each do |pin| %>
    <div class="pin-card">
      <%= link_to pin do %>
        <% if pin.image.attached? %>
          <%= image_tag pin.image %>
        <% end %>
      <% end %>
      <div class="pin-card-body">
        <p class="pin-description"><%= pin.description %></p>
        <p class="pin-user">Publicado por <%= pin.user.email %></p>
      </div>
    </div>
  <% end %>
</div>
