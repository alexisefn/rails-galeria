<div style="text-align: center; margin-bottom: 30px;">
  <h1>Pines de <%= @user.username %></h1>
  <p>Explora todas las imágenes que ha compartido <%= @user.username %>.</p>
</div>

<%# Para reutilizar la misma cuadrícula de la página principal %>
<div class="pins-container">
  <% @pins.each do |pin| %>
    <div class="pin-card">
      <%= link_to pin do %>
        <% if pin.image.attached? %>
          <%= image_tag pin.image %>
        <% end %>
      <% end %>
      <div class="pin-card-body">
        <p class="pin-description"><%= pin.description %></p>
        <%= render 'likes/like_button', pin: pin %>
        <%# En el perfil, no se necesita mostrar el autor de nuevo, ya que todos son del mismo usuario %>
      </div>
    </div>
  <% end %>
</div>
